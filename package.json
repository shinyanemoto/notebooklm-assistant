{
  "name": "notebooklm-assistant-extension",
  "version": "0.1.1",
  "private": true,
  "description": "NotebookLM workflow assistant Chrome extension (MVP)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.25",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2"
  }
}
